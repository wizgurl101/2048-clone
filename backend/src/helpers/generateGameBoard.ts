import { NUMBER_OF_COLUMNS, NUMBER_OF_ROWS } from "../constants/gameBoardConstants";

const generateNewBoard = () => {
    const board = [];

    for(let i = 0; i < NUMBER_OF_ROWS; i++) {
        board[i] = [];

        for(let j = 0; j < NUMBER_OF_COLUMNS; j++) {
            board[i][j] = 
        }
    }
}